services:
  AiVerse-Chat:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    ports:
      - '7070:7070'
    environment:
      - NO_HTTPS=1
    env_file:
      - .env
    dns:
      - 8.8.8.8 # Google's public DNS server
      - 8.8.4.4 # Google's public DNS server
    networks:
      - AiVerse-Chat-networks
    depends_on:
      - postgres
    restart: unless-stopped

  postgres:
    image: postgres:17
    env_file:
      - .env
    networks:
      - AiVerse-Chat-networks
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  AiVerse-Chat-networks:
    driver: bridge
